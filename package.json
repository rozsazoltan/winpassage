{
  "bugs": {
    "url": "https://github.com/rozsazoltan/winpassage/issues"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "homepage": "https://github.com/rozsazoltan/winpassage#readme",
  "license": "AGPL-3.0-only",
  "name": "winpassage",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/rozsazoltan/winpassage.git"
  },
  "scripts": {
    "build:admin": "aube run tauri:build --filter winpassage-admin",
    "build:agentctl": "cargo build --release -p winpassage-agentctl",
    "build:client": "aube run tauri:build --filter winpassage-client",
    "build:js": "aube run build --filter winpassage-admin && aube run build --filter winpassage-client",
    "build:server": "cargo build --release -p winpassage-server",
    "build:updater": "cargo build --release -p winpassage-updater",
    "check:all": "aube run check:rust && aube run check:js",
    "check:js": "aube run typecheck --filter winpassage-admin && aube run typecheck --filter winpassage-client && aube run test --filter winpassage-admin && aube run test --filter winpassage-client && aube run build --filter winpassage-admin && aube run build --filter winpassage-client",
    "check:rust": "cargo fmt --all -- --check && cargo check --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace --all-targets && cargo test --workspace --doc && cargo doc --workspace --no-deps",
    "dev:admin": "aube run tauri:dev --filter winpassage-admin",
    "dev:client": "aube run tauri:dev --filter winpassage-client",
    "fmt:rust": "cargo fmt --all",
    "fmt:rust:check": "cargo fmt --all -- --check",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "test:js": "aube run test --filter winpassage-admin && aube run test --filter winpassage-client",
    "test:rust": "cargo test --workspace",
    "typecheck:js": "aube run typecheck --filter winpassage-admin && aube run typecheck --filter winpassage-client"
  },
  "type": "module",
  "version": "0.3.0"
}
