[project]
root = "."
frontend_install = "aube install"

[build]
out_dir = "dist/release/winpassage-admin"
cache_dir = ".cache/tauri-release/winpassage-admin"
default_strategy = "host"
container_engine = "podman"

[artifacts]
checksum = true
manifest = true

[platforms.windows]
enabled = true
strategy = "host"
command = "aube run tauri:build --filter winpassage-admin"
artifacts = [
  ".cache/rust/packages/winpassage/target/release/bundle/nsis/WinPassageAdmin_*_x64-setup.exe",
  ".cache/rust/packages/winpassage/target/release/bundle/msi/WinPassageAdmin_*_x64_en-US.msi",
]

[platforms.linux]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-admin"
artifacts = []

[platforms.macos]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-admin"
artifacts = []

[platforms.android]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-admin"
artifacts = []

[platforms.ios]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-admin"
artifacts = []
