[project]
root = "."
frontend_install = "aube install"

[build]
out_dir = "dist/release/winpassage-client"
cache_dir = ".cache/tauri-release/winpassage-client"
default_strategy = "host"
container_engine = "podman"

[artifacts]
checksum = true
manifest = true

[platforms.windows]
enabled = true
strategy = "host"
command = "aube run tauri:build --filter winpassage-client"
artifacts = [
  ".cache/rust/packages/winpassage/target/release/bundle/**/*.msi",
  ".cache/rust/packages/winpassage/target/release/bundle/**/*.exe",
  ".cache/rust/packages/winpassage/target/release/bundle/**/*.zip",
  "apps/client/src-tauri/target/release/bundle/**/*.msi",
  "apps/client/src-tauri/target/release/bundle/**/*.exe",
  "apps/client/src-tauri/target/release/bundle/**/*.zip",
]

[platforms.linux]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-client"
artifacts = []

[platforms.macos]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-client"
artifacts = []

[platforms.android]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-client"
artifacts = []

[platforms.ios]
enabled = false
strategy = "host"
command = "aube run tauri:build --filter winpassage-client"
artifacts = []
